import 'package:flutter/cupertino.dart';

class Secreen extends StatelessWidget {
  const Secreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}